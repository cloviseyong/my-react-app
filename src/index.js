import React from 'react';
import ReactDOM from 'react-dom/client';

const myFirstElement = <h1> Welcomeon board</h1>

const myFirstElement1 = <h1> Welcomeon board</h1>

const root = ReactDOM.createRoot(document.getElementById('root'));root.render(myFirstElement);
const root1 = ReactDOM.createRoot(document.getElementById('root'));root.render(myFirstElement1);